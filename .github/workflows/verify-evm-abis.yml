name: Verify EVM ABIs

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify-abis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify abi/ matches dkg-evm-module.lock
        run: bash tools/evm/scripts/sync_abis.sh --check

